import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uni-supply-bo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
